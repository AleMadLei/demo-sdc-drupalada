/**
 * @file
 * drupalada_sdc behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.drupaladaSdc = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
